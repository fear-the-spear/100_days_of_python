Today's Learning Points

  • leverage the power of NumPy's ndarrays

  • access individual values and subsets inside an n-dimensional array

  • how broadcasting works with arrays

  • linear algebra with NumPy

  • generate points that you can plot on a chart

  • manipulate images as ndarrays

  • create arrays manually with np.array()

  • generate arrays using .arange(), .random(), and .linspace()

  • analyse the shape and dimensions of a ndarray

  • slice and subset a ndarray based on its indices

  • do linear algebra like operations with scalars and matrix multiplication

  • use NumPy's broadcasting to make ndarray shapes compatible

  • manipulate images in the form of ndarrays